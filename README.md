# ToDoListPHP
A simple To-Do List web application developed using PHP, MySQL, and Bootstrap. Users can add tasks, mark them as completed, and delete them. The application uses a database to store tasks permanently and follows a clean folder structure suitable for academic projects.
